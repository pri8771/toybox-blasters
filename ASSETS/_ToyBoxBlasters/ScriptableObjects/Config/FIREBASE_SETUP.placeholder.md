# Firebase setup (placeholder)

**Task 001:** Backend preference is **Firebase**. Do not commit production secrets.

1. Create a Firebase project for *ToyBox Blasters*.
2. Add iOS and Android apps; download config files.
3. Place `google-services.json` (Android) and `GoogleService-Info.plist` (iOS) via Unity Firebase SDK workflow.
4. Implement `IAuthService`, `IAnalyticsService`, etc. against Firebase in a later task.

Files are gitignored — see root `.gitignore`.
